#include "main.h"
/**
 * mul - takes the multiple of 2 numbers
 * @a: first int entered by user
 * @b: second int entered by user
 * Return: value of a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
